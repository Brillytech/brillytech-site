// Site-only launch stubs. Replace with real Supabase exports later.

export { createClient as supabaseBrowser } from "./browser";
export { supabaseServer } from "./server";
export { supabaseAdmin } from "./admin";
